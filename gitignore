# Prevent generated content from being committed to the repository

# Ignore permalink directories and timestamped archives, which are generated by
# the TEK2day Pulse pipeline and can grow rapidly. These should not be
# committed to the git history; they are pruned by scripts/prune_generated.py
docs/p/
docs/archive/timestamped/

# Optionally ignore local cache data (uncomment to enable)
# docs/.cache/

# Python cache files
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
.env
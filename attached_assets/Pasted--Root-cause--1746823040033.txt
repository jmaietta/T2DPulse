| Root cause                                                                                                                                                                                  | Evidence                                               | Recommendation                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **a. `create_pulse_card` re‑reads the full 30‑day history file on every invocation** and converts the date column each time .                                                               | File I/O + date parsing ≈ several hundred ms.          | *Load once.*  Read `data/t2d_pulse_history.csv` at app start‑up (next to the other global loads) and keep it in a module‑level variable; hand that DataFrame to `create_pulse_card`.                                                         |
| **b. The same callback runs multiple times per refresh.**  `update_sentiment_gauge` is triggered by changes to hidden divs and again by the hourly interval, repeating the expensive work . | Duplicate compute cost.                                | Add `@dash.callback(..., prevent_initial_call=True)` to the redundant trigger or move the pulse‑score calculation into a memoised helper (`@cache.memoize(timeout=300)`).                                                                    |
| **c. Plotly figure complexity.**  Even after optimisation the pulse sparkline still draws 165 px of line/fill on every refresh and includes hover templates (disabled later).               | All that DOM diffing takes > 400 ms on slower laptops. | - Switch to `px.area(..., height=150)` with `staticPlot=True, displayModeBar=False` so Plotly skips interactivity.<br>- Or pre‑render the sparkline to a PNG once a day and serve it via `html.Img`, updating only when new history appears. |
